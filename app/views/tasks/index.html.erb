
<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %>
  <% end %>
</ul>

<%= link_to "Add a new task", new_path %>

<%# <%= link_to "Add a new task",
  tasks_path(@task),
  data: {turbo_method: :add}
%>
